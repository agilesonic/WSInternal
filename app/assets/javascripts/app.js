Ext.application({
    name: 'WSIS',
	appFolder: '/assets/app',

    // automatically create an instance of AM.view.Viewport
    autoCreateViewport: true,

    controllers: [
        'Clients'
    ],
    
    launch: function() {
    }
});
